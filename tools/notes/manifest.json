{
  "name": "notes",
  "description": "Keep short freeform notes that outlive a conversation.",
  "db_prefix": "notes_",
  "timeout_seconds": 15,
  "parameters": {
    "type": "object",
    "properties": {
      "action": { "type": "string", "enum": ["add", "list"] },
      "text": { "type": "string", "description": "The note, when adding." }
    },
    "required": ["action"]
  }
}
