{
  "name": "bash",
  "description": "Run a shell command in the workspace. The shell is sandboxed: only the workspace is writable, so put scratch files under $TMPDIR rather than /tmp, and /proc cannot be read, so a browser will not start here. To read a web page, use web_fetch or web_browse instead of running a browser from the shell. curl, wget, python3, and perl are installed.",
  "db_prefix": "bash_",
  "timeout_seconds": 150,
  "parameters": {
    "type": "object",
    "properties": {
      "command": {
        "type": "string",
        "description": "Shell command."
      },
      "timeout_seconds": {
        "type": "integer"
      }
    },
    "required": [
      "command"
    ]
  }
}
